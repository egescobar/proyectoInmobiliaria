import { FiltroPrecioPipe } from './filtro-precio.pipe';

describe('FiltroPrecioPipe', () => {
  it('create an instance', () => {
    const pipe = new FiltroPrecioPipe();
    expect(pipe).toBeTruthy();
  });
});
